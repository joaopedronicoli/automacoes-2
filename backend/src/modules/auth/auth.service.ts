import { Injectable } from '@nestjs/common';

@Injectable()
export class AuthService {
    // Auth is now managed by Supabase.
    // This service is kept as a placeholder for any future backend-only auth logic.
}
